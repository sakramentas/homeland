import React from 'react';
import './styles.css';

const PropertyTag = (props) => {
  return (
    <div className="property-tag">
      <span>The property matches your profile</span>
    </div>
  );
};

export default PropertyTag;
